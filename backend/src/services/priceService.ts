import type { MaterialCategory, PrismaClient, LotCondition } from '@prisma/client'; import { AppError } from '../utils/errors.js'; import { PriceRepository } from '../repositories/priceRepository.js';
const multipliers: Record<LotCondition, number> = { INTACT: 1, DAMAGED: .7, PARTIAL: .4 };
const validMoney = (n:number) => Number.isFinite(n) && n > 0 && n < 1000000;
export class PriceService { constructor(private readonly repo: PriceRepository, private readonly db: PrismaClient) {} private async selected(material:MaterialCategory, location?:string) { const price = await this.repo.latest(material, location); if (!price) throw new AppError('NOT_FOUND','No price available for this material and location',404,{ code:'PRICE_NOT_FOUND' }); return price; } private trend(current:number, history:{marketPrice:number}[]) { const prior = history.slice(0,-1); if (!prior.length) return { direction:'STABLE', percentage:0 }; const avg = prior.reduce((s,p)=>s+p.marketPrice,0)/prior.length; const percentage = avg ? Number((((current-avg)/avg)*100).toFixed(2)) : 0; return { direction: percentage > 1 ? 'UP' : percentage < -1 ? 'DOWN' : 'STABLE', percentage }; } async board(material:MaterialCategory, location?:string) { const price=await this.selected(material,location); const history=await this.repo.history(material, location, new Date(Date.now()-30*86400000)); return { materialCategory:material, location: price.areaName ?? price.city, priceMin:price.priceMin, priceMax:price.priceMax, marketPrice:price.marketPrice, historicalAverage:price.historicalAverage, trend:this.trend(price.marketPrice,history), lastUpdated:price.effectiveAt.toISOString(), disclaimer:'Development/trusted source data; not real-time.' }; } async history(material:MaterialCategory, location:string|undefined, days:number) { return { materialCategory:material, location:location ?? null, days, history:(await this.repo.history(material,location,new Date(Date.now()-days*86400000))).map(p=>({ date:p.effectiveAt.toISOString(), marketPrice:p.marketPrice })) }; } async valuation(lotId:string, collectorId:string) { const lot=await this.db.lot.findFirst({where:{id:lotId,collectorId}}); if(!lot) throw new AppError('NOT_FOUND','Lot not found',404,{code:'LOT_NOT_FOUND'}); const price=await this.selected(lot.materialCategory,lot.collectionAreaName ?? undefined); const value=Number((price.marketPrice*lot.weight*multipliers[lot.condition]).toFixed(2)); await this.db.lot.update({where:{id:lot.id},data:{estimatedValue:value}}); return {lotId,basePricePerKg:price.marketPrice,weight:lot.weight,conditionMultiplier:multipliers[lot.condition],qualityAdjustment:1,estimatedValue:value,priceDate:price.effectiveAt.toISOString(),disclaimer:'Estimate only; quality adjustment is neutral until trusted classification is available.'}; } async update(id:string, actor:string, data:{priceMin:number;priceMax:number;marketPrice:number;reason?:string}) { if(!validMoney(data.priceMin)||!validMoney(data.priceMax)||!validMoney(data.marketPrice)||data.priceMin>data.priceMax||data.marketPrice<data.priceMin||data.marketPrice>data.priceMax) throw new AppError('VALIDATION_ERROR','Invalid price range',422,{code:'INVALID_PRICE'}); const result=await this.repo.update(id,actor,data); if(!result) throw new AppError('NOT_FOUND','Price not found',404,{code:'PRICE_NOT_FOUND'}); return result; } }
