import { Router } from 'express'; import { collectorController } from '../controllers/collectorController.js'; import { requireAuth } from '../middleware/auth.js'; import type { JwtService } from '../services/jwt.js'; import type { CollectorService } from '../services/collectorService.js'; import type { CollectorRepository } from '../repositories/collectorRepository.js';
export const collectorRoutes = (jwt: JwtService, collectors: CollectorRepository, service: CollectorService) => { const router = Router(); const controller = collectorController(service); return router.get('/me', requireAuth(jwt, collectors), controller.me).put('/me', requireAuth(jwt, collectors), controller.update); };
